apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app-generic.fullname" . }}-config
  labels:
    {{- include "app-generic.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configData }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
