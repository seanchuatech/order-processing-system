apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: order-service
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm-charts/app-generic
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: order-service-values
      valuesKey: order-service-values.yaml
